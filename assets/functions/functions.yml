functions:
  alarm:
    topic: "alarm"
    description: "Set an alarm for a specific time in seconds"
    parameters:
      - parameter:
          name: "minutes"
          required: true

  bedroom-light:
    topic: "bedroom_light"
    description: "Turn the light in my bedroom on or off"
    parameters:
      - parameter:
          name: "enabled"
          values:
            - true
            - false
          required: true